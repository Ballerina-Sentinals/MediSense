-- DELIMITER $$

-- CREATE PROCEDURE CreateReminder(
--     IN p_user_id INT,
--     IN p_date DATE,
--     IN p_time TIME,
--     IN p_med_ids VARCHAR(255)
-- )
-- BEGIN
--     INSERT INTO Reminders (user_id, date, time, med_ids)
--     VALUES (p_user_id, p_date, p_time, p_med_ids);
-- END $$

-- DELIMITER ;


